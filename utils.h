#include <stdint.h>

uint32_t ipow(uint32_t, uint8_t);

uint8_t get_digit_from_char(uint8_t);
uint16_t get_number_from_string(uint8_t [ ], bool);
bool is_number(uint8_t [ ]);

uint8_t* get_string_from_number(uint32_t);
